// import { createSelector, Selector } from "reselect";
import { State } from "../index";
//
// const selectUserState = (state: State) => state.userReducer;
//
// export const selectIsAuth: Selector<State, boolean> = createSelector(
//   selectUserState,
//   ({ isAuth }) => isAuth
// );
